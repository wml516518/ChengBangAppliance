@{
    ViewData["Title"] = "首页";
    var isLoggedIn = User.Identity?.IsAuthenticated == true;
}
<div class="card">
    <h1>欢迎使用诚邦家电维修管理系统</h1>
    @if (isLoggedIn)
    {
        <p>您好，<strong>@User.Identity?.Name</strong>，欢迎回来。</p>
        <h3>系统说明</h3>
        <ul>
            <li><strong>商品列表</strong>：可浏览已上架的家电维修相关商品与服务。</li>
            <li><strong>商品管理</strong>（管理员）：添加、编辑、上下架商品，并支持上传商品照片。</li>
            <li><strong>自建工单</strong>（管理员）：手动创建服务工单，支持选择订单类型和服务项目，填写联系人信息并指派师傅。</li>
            <li><strong>服务类型</strong>（管理员）：维护订单类型（安装、维修等）和对应的服务项目。</li>
            <li><strong>师傅管理</strong>（管理员）：管理技师账号，支持新建或从已有用户中设置。</li>
        </ul>
        <p>
            <a href="/Products" class="btn btn-primary">浏览商品列表</a>
            @if (User.IsInRole("Admin"))
            {
                <a href="/ManualOrders/Create" class="btn btn-primary" style="margin-left:8px;">创建工单</a>
            }
        </p>
    }
    else
    {
        <p>请先 <a href="/Account/Login">登录</a> 或 <a href="/Account/Register">注册</a>。</p>
        <p><a href="/Products" class="btn btn-primary">浏览商品列表</a></p>
    }
</div>
