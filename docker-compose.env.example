# 生产环境可复制为 .env 并修改
# 复制: cp docker-compose.env.example .env

# 对外 HTTP 端口（默认 80）
HTTP_PORT=80

# JWT 配置（务必修改为随机长字符串）
JWT_KEY=your-production-jwt-secret-key-at-least-32-characters!!
JWT_ISSUER=ApplianceRepair
JWT_AUDIENCE=ApplianceRepair
